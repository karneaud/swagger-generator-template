<?php
namespace {{ namespace }};

use {{ namespace }}\Message\Request\RequestInterface;

abstract class AbstractClient implements ClientInterface
{
  
  protected $default_parameters = [];
  
  public function getDefaultParameters() : array {
        $this->default_parameters;
  }

  public function setDefaultParameters(array $params) { 
    $this->default_parameters = $params;
  }
  
  abstract protected function createRequest(string $class_name, array $parameters ) :RequestInterface;
}
