<?php
namespace {{ namespace }};

use {{ namespace }}\Message\Request\RequestInterface;

trait ApiTrait
{
  {% for method,func in methods %}
  public function {{ method }}{{ func.name }}({{ func.has_params? 'array \$params' : '' }}) : ResponseInterface {
    return $this->createRequest('{{func.name}}',$params)->send();  
  }
  {% endfor %}
 }
